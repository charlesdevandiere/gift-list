name: gift-list

services:
  giftlist:
    build: .
    container_name: gift-list
    image: gift-list:latest
    ports:
      - 3000:3000
    volumes:
      - type: bind
        source: ./.env
        target: /home/gift-list/app/.env
        read_only: true
      - type: bind
        source: /var/log/gift-list
        target: /var/log/gift-list
        read_only: false
